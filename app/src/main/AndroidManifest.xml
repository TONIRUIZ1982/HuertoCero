<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_huerto_cero_mark"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_huerto_cero_mark"
        android:supportsRtl="true"
        android:theme="@style/Theme.HuertoCero">

        <!-- LOGIN (ARRANQUE) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- RESTO -->
        <activity android:name=".OnboardingActivity"/>
        <activity android:name=".MapActivity"/>
        <activity android:name=".FavoritesActivity"/>
        <activity android:name=".ReservationsActivity"/>
        <activity android:name=".ChatsActivity"/>
        <activity android:name=".ChatActivity"/>
        <activity
            android:name=".DetailActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="OPEN_PRODUCT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".PrivacyActivity"/>
        <activity android:name=".MyProductsActivity"/>
        <activity android:name=".SellerProfileActivity"/>

        <service
            android:name=".HuertoMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- GOOGLE MAPS -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBXA87ua9cM2yYa8ieFJT45h-1gxzVynMQ"/>

    </application>

</manifest>
